# ------ PRISMA GENERATED DOTENV ------
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema
# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings
DATABASE_URL="file:./database.db" # do not mofify the schema.
# ------ PRISMA GENERATED DOTENV ------

# JWT # https://www.npmjs.com/package/jsonwebtoken
JWT_SECRET_TOKEN="YOUR SECRET TOKEN HERE" # secretOrPrivateKey when signing and verifying, can be generated with e.g: require('crypto').randomBytes(32).toString('hex')
JWT_LIFETIME="24h" # 'expiresIn' option when signing

# Web Server
BACKEND_HOST="127.0.0.1" # loopback: 127.0.0.1, expose to net: 0.0.0.0
BACKEND_PORT=8585 # Any value above 1000 should be fine. max=65535
CORS_ALLOW_ORIGIN="*" # Should be front end server address for security. e.g: http://localhost:8686
